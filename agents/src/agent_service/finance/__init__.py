"""Everything the finance agent owns: its agent, prompts, tools, models, queries.

The template for gmail and notion. A feature package holds its own vocabulary and
its own database access; only genuinely shared infrastructure lives at the root.
"""
